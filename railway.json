{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd trading-bot && pip install --no-cache-dir -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd trading-bot && python api.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
