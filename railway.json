{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd trading-bot && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd trading-bot && python api.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
